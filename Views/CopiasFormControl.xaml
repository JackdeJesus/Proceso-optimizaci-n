<UserControl x:Class="PoderJudicial.Views.CopiasFormControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      HorizontalAlignment="Stretch"
      VerticalAlignment="Stretch"
      Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource InputBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2ECC8F"/>
                                <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputStyleReadOnly" TargetType="TextBox"
               BasedOn="{StaticResource InputStyle}">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
        </Style>

        <Style x:Key="ComboStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource InputBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="40"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{TemplateBinding SelectionBoxItem}"
                                               Margin="12,0,0,0"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Left"
                                               Foreground="{DynamicResource PrimaryTextBrush}"/>
                                    <TextBlock Grid.Column="1"
                                               Text="▼"
                                               FontSize="10"
                                               Foreground="{DynamicResource PrimaryTextBrush}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                    <ToggleButton Grid.ColumnSpan="2"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Focusable="False"
                                                  IsChecked="{Binding IsDropDownOpen,
                                                  RelativeSource={RelativeSource TemplatedParent},
                                                  Mode=TwoWay}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Slide">
                                <Border Background="{DynamicResource CardBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="0,4,0,0">
                                    <ScrollViewer MaxHeight="250">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2ECC8F"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2ECC8F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2ECC8F"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ══════════════════════════════════
             HEADER
        ══════════════════════════════════ -->
        <Border Grid.Row="0"
                Background="{DynamicResource CardBrush}"
                BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource BorderBrush}"
                Padding="32,18"
                Margin="0,0,0,30">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Registro de Copias"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <TextBlock Text="Complete los campos para registrar la entrega de copias."
                               FontSize="12"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               Margin="0,3,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock x:Name="TxtHora"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               HorizontalAlignment="Right"/>
                    <TextBlock x:Name="TxtFecha"
                               FontSize="12"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               HorizontalAlignment="Right"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ══════════════ SCROLL CONTENIDO ══════════════ -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border Background="{DynamicResource CardBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="16"
                    Padding="36,32"
                    Margin="35,0,35,35">
                <StackPanel>

                    <!-- ─── FILA 1: ID | no. Causa | Fecha Recibo ─── -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- ID -->
                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Id" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text=" *" Foreground="#EF4444" FontWeight="Bold" Margin="0,0,0,6"/>
                            </StackPanel>
                            <TextBox x:Name="TxtId"
                                     Style="{StaticResource InputStyleReadOnly}"/>
                        </StackPanel>

                        <!-- No. Causa -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="No. Causa" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="TxtNoCausa"
             Style="{StaticResource InputStyle}"
             Tag="Ej: 123/2024"
             PreviewTextInput="NoCausa_PreviewTextInput"
             LostFocus="TxtNoCausa_LostFocus"/>
                        </StackPanel>

                        <!-- Fecha Recibo -->
                        <StackPanel Grid.Column="4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Fecha de Recibo" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text=" *" Foreground="#EF4444" FontWeight="Bold" Margin="0,0,0,6"/>
                            </StackPanel>
                            <TextBox x:Name="TxtFeRecibo"
                                     Style="{StaticResource InputStyleReadOnly}"/>
                        </StackPanel>
                    </Grid>

                    <!-- ─── SEPARADOR ─── -->
                    <Separator Background="{DynamicResource BorderBrush}" Margin="0,4,0,20"/>

                    <!-- ─── FILA 2: Fecha Audiencia | NUC | Tipo Causa ─── -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Fecha Audiencia (autocompletada desde No. Causa, solo lectura) -->
                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Fecha Audiencia" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text=" *" Foreground="#EF4444" FontWeight="Bold" Margin="0,0,0,6"/>
                            </StackPanel>
                            <TextBox x:Name="TxtFeAudiencia"
             Style="{StaticResource InputStyleReadOnly}"
             Tag="Se completa según No. Causa"/>
                            <!--
        Se muestra en vez del TextBox de arriba solo
        cuando el No. Causa tiene más de una audiencia
        registrada (varios discos originales posibles):
        el usuario elige a cuál corresponde la copia.
        Al seleccionar, se refleja en TxtFeAudiencia
        (ver CmbFeAudienciaOpciones_SelectionChanged),
        así que el resto de la lógica de guardado no
        necesita saber que esto existe.
    -->
                            <ComboBox x:Name="CmbFeAudienciaOpciones"
              Style="{StaticResource ComboStyle}"
              Visibility="Collapsed"
              SelectionChanged="CmbFeAudienciaOpciones_SelectionChanged"/>
                        </StackPanel>

                        <!-- NUC (autocompletado desde No. Causa, solo lectura) -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="NUC" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="TxtNUC"
                                     Style="{StaticResource InputStyleReadOnly}"
                                     Tag="Se completa según No. Causa"/>
                        </StackPanel>

                        <!-- Tipo Causa (autocompletado desde No. Causa, solo lectura) -->
                        <StackPanel Grid.Column="4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Tipo Causa" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text=" *" Foreground="#EF4444" FontWeight="Bold" Margin="0,0,0,6"/>
                            </StackPanel>
                            <TextBox x:Name="TxtTipoCausa"
                                     Style="{StaticResource InputStyleReadOnly}"
                                     Tag="Se completa según No. Causa"/>
                        </StackPanel>
                    </Grid>

                    <!-- ─── FILA 3: Total Discos Entregados | Tipo Disco | Discos Externos ─── -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Total Discos Entregados -->
                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Total Discos Entregados" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text=" *" Foreground="#EF4444" FontWeight="Bold" Margin="0,0,0,6"/>
                            </StackPanel>
                            <ComboBox x:Name="CmbTotDiscosEntregados"
                                      Style="{StaticResource ComboStyle}"
                                      SelectedIndex="0">
                                <ComboBoxItem Content="Seleccione cantidad"/>
                                <ComboBoxItem Content="1 disco"/>
                                <ComboBoxItem Content="2 discos"/>
                                <ComboBoxItem Content="3 discos"/>
                                <ComboBoxItem Content="4 discos"/>
                                <ComboBoxItem Content="5 discos"/>
                                <ComboBoxItem Content="6 discos"/>
                                <ComboBoxItem Content="7 discos"/>
                                <ComboBoxItem Content="8 discos"/>
                                <ComboBoxItem Content="9 discos"/>
                                <ComboBoxItem Content="10 discos"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Tipo Disco -->
                        <StackPanel Grid.Column="2">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Tipo de Disco" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text=" *" Foreground="#EF4444" FontWeight="Bold" Margin="0,0,0,6"/>
                            </StackPanel>
                            <ComboBox x:Name="CmbTipoDisco"
               Style="{StaticResource ComboStyle}"
               SelectedIndex="0">
                                <ComboBoxItem Content="Seleccione tipo"/>
                                <ComboBoxItem Content="Copia Simple"/>
                                <ComboBoxItem Content="Copia Autentica"/>

                            </ComboBox>
                        </StackPanel>

                        <!-- Discos Externos -->
                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Discos Externos" Style="{StaticResource LabelStyle}"/>
                            <ComboBox x:Name="CmbDiscosExternos"
                                      Style="{StaticResource ComboStyle}"
                                      SelectedIndex="0">
                                <ComboBoxItem Content="Ninguno"/>
                                <ComboBoxItem Content="1 disco"/>
                                <ComboBoxItem Content="2 discos"/>
                                <ComboBoxItem Content="3 discos"/>
                                <ComboBoxItem Content="4 discos"/>
                                <ComboBoxItem Content="5 discos"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <!-- ─── SEPARADOR ─── -->
                    <Separator Background="{DynamicResource BorderBrush}" Margin="0,4,0,20"/>

                    <!-- ─── FILA 4: Etiquetas Entregadas | A Quien se Entrega ─── -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Etiquetas Entregadas -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Etiquetas Entregadas" Style="{StaticResource LabelStyle}"/>
                            <ComboBox x:Name="CmbEtiquetasEntregadas"
                                      Style="{StaticResource ComboStyle}"
                                      SelectedIndex="0">
                                <ComboBoxItem Content="Ninguna"/>
                                <ComboBoxItem Content="1 etiqueta"/>
                                <ComboBoxItem Content="2 etiquetas"/>
                                <ComboBoxItem Content="3 etiquetas"/>
                                <ComboBoxItem Content="4 etiquetas"/>
                                <ComboBoxItem Content="5 etiquetas"/>
                                <ComboBoxItem Content="6 etiquetas"/>
                                <ComboBoxItem Content="7 etiquetas"/>
                                <ComboBoxItem Content="8 etiquetas"/>
                                <ComboBoxItem Content="9 etiquetas"/>
                                <ComboBoxItem Content="10 etiquetas"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- A Quien se Entrega -->
                        <StackPanel Grid.Column="2">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="A Quien se Entrega" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text=" *" Foreground="#EF4444" FontWeight="Bold" Margin="0,0,0,6"/>
                            </StackPanel>
                            <TextBox x:Name="TxtAQuienSeEntrega"
                                     Style="{StaticResource InputStyle}"
                                     Tag="Nombre de quien recibe"
                                     TextChanged="TxtAQuienSeEntrega_TextChanged"
                                     PreviewKeyDown="TxtAutocomplete_PreviewKeyDown"/>
                            <ListBox x:Name="lstAQuienSeEntrega"
                                     MouseDoubleClick="lstAutocomplete_MouseClick"
                                     PreviewKeyDown="lstAutocomplete_PreviewKeyDown"
                                     Visibility="Collapsed"
                                     Height="120"/>
                        </StackPanel>

                    </Grid>

                    <!-- ─── SEPARADOR ─── -->
                    <Separator Background="{DynamicResource BorderBrush}" Margin="0,4,0,20"/>

                    <!-- ─── FILA 5: Observaciones (ancho completo) ─── -->
                    <StackPanel Margin="0,0,0,32">
                        <TextBlock Text="Observaciones" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="TxtObservaciones"
                                 Height="100"
                                 FontSize="13"
                                 Foreground="{DynamicResource PrimaryTextBrush}"
                                 CaretBrush="{DynamicResource PrimaryTextBrush}"
                                 Background="{DynamicResource InputBrush}"
                                 BorderBrush="{DynamicResource BorderBrush}"
                                 BorderThickness="1"
                                 Padding="12,10"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 Tag="Escriba observaciones adicionales...">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Style.Triggers>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter Property="BorderBrush" Value="#2ECC8F"/>
                                            <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="8"
                                            Padding="{TemplateBinding Padding}">
                                        <ScrollViewer x:Name="PART_ContentHost"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter Property="BorderBrush" Value="#2ECC8F"/>
                                            <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                    </StackPanel>

                    <!-- ─── FILA 6: ¿Se grabó directo? (afecta "Quien Realiza") ─── -->
                    <Grid Margin="0,0,0,32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="¿Se grabó directo?" Style="{StaticResource LabelStyle}"/>
                            <ComboBox x:Name="CmbGrabadoDirecto"
                                      Style="{StaticResource ComboStyle}"
                                      SelectedIndex="0">
                                <ComboBoxItem Content="No"/>
                                <ComboBoxItem Content="Sí"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <!-- ─── SEPARADOR ─── -->
                    <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,24"/>

                    <!-- ─── BOTÓN GUARDAR ─── -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="BtnGuardar"
                                Content="Guardar Registro"
                                Height="44"
                                Width="180"
                                Cursor="Hand"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Click="BtnGuardar_Click">
                            <Button.Foreground>White</Button.Foreground>
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="BtnBorder"
                                            Background="#2ECC8F"
                                            CornerRadius="10">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="BtnBorder" Property="Background" Value="#27B57D"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="BtnBorder" Property="Background" Value="#1F9E6C"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>

                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>